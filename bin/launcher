#!/bin/bash
# Maintainer: José Moreira <jose.moreira@findhit.com>

VERSION=$1
PWD=$PWD

throw () {
    echo ${@:2}
    exit $1
}

[ -z "$(which docker)" ] && {
    throw 1 "Please make sure you have docker installed"
}

## Actions

build () {
    NODE_VERSION=$1
    IMAGE_NAME=$2

